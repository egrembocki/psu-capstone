name: htmrlenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - numpy           
  - pandas          
  - pip:
      - pre-commit
      - pytest
      - mmh3
      - pdoc
      - flake8
      - black
      - randomname
      - openpyxl
      - mmh3
      - pre-commit
      - pytest
      
      
